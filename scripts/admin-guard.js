/**
 * Admin Guard — stub pendiente de implementación.
 * Este archivo existe para evitar errores 404 en las páginas que lo referencian.
 * La lógica real de protección de rutas administrativas (verificación de rol
 * en Firestore) se implementará en una fase posterior, una vez configuradas
 * las credenciales de Firebase y definida la estructura de roles.
 */

export function checkAdminAccess() {
    // Sin implementación funcional aún.
}
