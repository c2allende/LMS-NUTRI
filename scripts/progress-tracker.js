/**
 * Progress Tracker — stub pendiente de implementación.
 * Este archivo existe para evitar errores 404 en las páginas que lo referencian.
 * La lógica real de seguimiento de progreso en Firestore se implementará
 * en una fase posterior, una vez configuradas las credenciales de Firebase.
 */

export function trackProgress(pageId, moduleId) {
    // Sin implementación funcional aún.
}

export function getProgress(moduleId) {
    // Sin implementación funcional aún.
    return null;
}
